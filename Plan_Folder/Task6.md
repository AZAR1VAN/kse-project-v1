# Task 6 — Test & verify (R6)

## Опис
Юніт-тести логіки + UI-верифікація через Playwright (скріншоти кожної сторінки) у goal-driven циклі (Karpathy): фіксуй ціль, перевіряй, виправляй до виконання.

## Обсяг
- `pytest tests/` — data, analysis, agents (fallback).
- Playwright (через `playwright-skill`): авто-детект порту Streamlit, відкрити кожну сторінку, дочекатися графіків, скріншот у `/tmp/airalerts-*.png`, перевірити відсутність тексту трейсбеку/«Error».
- `graphify .` для швидкої навігації кодом при виправленнях.

## Критерії готовності
- Усі pytest зелені.
- Скріншоти всіх 7 в'юх (Home + 6 сторінок) без трейсбеків, з видимими графіками.
- Виявлені візуальні/логічні дефекти виправлено й перевірено повторно.
